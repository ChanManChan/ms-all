// export something that has the same kind of structure as the "stripe.ts" file

export const stripe = {
  charges: {
    // mockResolvedValue() <- makes sure that whenever we call the create function, we are going to get back a promise that automatically resolves itself with an empty object
    create: jest.fn().mockResolvedValue({})
  }
}
